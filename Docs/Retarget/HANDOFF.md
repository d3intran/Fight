# 会话交接 —— LOL → 2XKO 重定向

> 更新时间：2026-09-19 12:50 GMT+8
> **路线已修正：停止角度扫描，先定位错误层级 + 只校准 idle1/run + 换验收标准。**
> 开工前先读 `AGENTS.md §0`（当前路线与验收盲区）；坑列表与脚本索引也在 AGENTS.md。

---

## 0. 一句话现状

**基础链路（导入 / 重定向 / 导出 / 修缩放）已打通；动作质量与运动闭环尚未通过。**
所有 `Anims_TP_*`（含 v2）都算**实验产物**，不是验收合格的动画。
**目标仍然是：一个正常站立 + 一个正常移动，握斧在训练场跑通。**

---

## 1. 客观状态

| 资产 | 路径 | 状态 |
|---|---|---|
| 源骨架+网格+46 动画 | `/Game/Character/Darius/LOL_Source/` | ✅ 48 个已落盘 |
| `IK_LOL_Source` / `IK_Darius_Target` / `RTG_LOL_to_Darius` | `/Game/Character/Darius/Retarget/` | ✅ 链已映射；retarget root：源 `Root` / 目标 `root` |
| **`Anims_TP_v2/`** | `/Game/Character/Darius/Anims_TP_v2/` | ⚠️ **实验产物（当前最好）**：c6 姿势 + uL yaw42 + neck_01 yaw30 + 脚 twist + scale 已修 |
| `Anims_TP_tw_feet/` `Anims_TP_best/` `Anims_TP_t3/` | 同上 | 前几版对照 |
| `Anims_TP_*` 实验目录 60+ | 同上 | **全部实验产物**（已 gitignore，选定赢家后清） |
| `ABP_Darius_Test` | `/Game/Character/Darius/Blueprints/` | ✅ 已挂 `CharacterMesh0`（此前挂骨架不匹配的 `ABP_Unarmed` ⇒ 关卡里显示 A-pose） |

### v2 实测（`ik_37`，**两个动作都要看**）

| 段 | `idle1` 均值 | **`run` 均值 / 最大** |
|:---|---:|---:|
| 大腿L/R | 0.01 / 0.01 | 0.01 / 0.01 |
| 小腿L/R | 5.77 / 4.94 | 6.17 / 5.15 |
| 大臂L / 小臂L | 21.34 / 10.21 | 13.21 / 21.97（最大 37.4） |
| 大臂R / 小臂R | 17.85 / 16.79 | 16.93 / 12.22 |
| 锁骨L | **2.85** | **29.96 / 40.12** ← 站立好 ≠ 跑动好 |
| 锁骨R | 22.58 | 19.95 / 36.84 |
| 躯干 / 颈 / 头 | 21.85 / 15.68 / 24.20 | 20.63 / 21.38 / 29.09 |
| 肘面L / 肘面R | 36.68 / 2.09 | 42.51 / 20.27 |
| **颈面** | **6.01** | **73.77 / 141.69** ← 典型反例 |
| 脚L / 脚R | 5.06 / 15.64 | 5.06 / 15.64 |
| 手L / 手R | 58.21 / 34.59（存疑） | 30.49 / 55.82（存疑） |

⚠️ 之前报的「颈面 6.01 ✅」只取了 `idle1`：**`exp_cycle.py:477/483` 汇总只读 `idle1`**（已确认的偏差）。
⚠️ `ik_11_batch_retarget.py:53` 用 `endswith("run")` 选动作，实际混入 4 个技能动作，未进验收。
⚠️ 判据只比 `dir(父→子)`：**不测落点**。目标髋宽比源窄 22% ⇒ 方向全对也可能夹腿/交叉（**未实测确认**）。

---

## 2. 已证 / 已否（别重复试）

**已证（可复用）**
- Retarget Pose：**先 `auto_align_all_bones` 定大面，再逐骨写**（顺序反 = 没写）；pose 存在 **IK Rig** 里
  （三个资产都要存）；**验收必须跟在导出之后**
- 层级补偿有效：`spine_03` yaw+48 修锁骨/颈/头；`upperarm_l/r` **镜像 ±yaw** 补偿下游；`lowerarm_l` +30 修小臂L
- 脚部 twist：约定 = **self**，`Q = rot_diff(d_tgt, d_src)` 折进 offset（脚L 66.6→5.1、脚R 44.4→15.6）；
  修正已累加在 `Saved/pose_extra.json`，随 `ik_48` 还原自动带上
- `neck_01` +30 yaw 把「颈面」94.8→6.0 ⇒ **颈面不是冗余量**，它量的是头绕颈轴的**平面扭转**
- `upperarm_l` yaw 平衡点 42（+77 过冲 44.7 / +35 伤小臂）

**已否（别再试）**
- 用 `Q` 折 offset 修上半身方向（`fx_s03`/`fx_neck`，三种参照系都不对）
- 补链 / 拉长链改 `CHAIN_TO_CHAIN`（`ik_55/56`）—— 只能说那几次配置没成，
  **不能推出链映射不重要**
- `LOCAL_ROTATION_AXES` 全面劣化；`spine_01` 三轴 +30 探针全恶化（躯干要试只能负向小步长）
- 手部 twist（`hand_l→middle_01_l` 与 `L_Hand→L_Middle1` 疑似不对应，2XKO 多一根掌骨）⇒ 手L/手R 先别当缺陷

**结论强度提醒**
- AnimBP 不兼容**只能解释关卡里的 A-pose**，不能解释 Persona 单独播放时的交叉腿
- 「腿方向 0.01°」只能证明方向对，**不能证明落点对**
- 「补链无效」≠ 链映射不重要；「6/6 缩放正确、动画在动」≠ 姿态正常

---

## 3. 下一步四步（按顺序，不跳步）

1. **定位错误第一次出现在哪一层** —— 同一 `run` 同帧对照：
   源动画 → 净化后 UE 源 → 重定向预览 → 导出资产 → 游戏角色。
   源错查转换，预览错查重定向，导出错查后处理，只有角色错才查 AnimBP。
2. **只校准 `idle1 + run`，两个同时验收** —— 先敲定左右 / 前向 / 单位 / `root` 与 `pelvis` 语义
   （源 `Root` 在髋附近，目标 `root` 在原点，**不能只凭名字认为同义**）；脊柱比对应躯干跨度；
   手脚用核实过的多点朝向。
3. **校准与适配分开** —— 步宽、落脚点、膝向、骨盆起伏属于适配，必要时 IK/Control Rig 接触约束；
   原地动画 ≠ 骨盆没有有效平移；不直接给双腿加固定外展角，也不用运行时补丁掩盖错误映射。
4. **换验收标准后再恢复批处理** —— **全帧**检查：无异常交叉 / 无膝翻转 / 脚底合理 /
   握持稳定 / 循环连续；然后才是方向误差，且 idle1 与 run 都报。任一关键项失败即停止扩量。

**建议补的判据（现有判据完全没测）**：身体坐标系下左右脚横向次序与间距、腿段相交、
支撑脚漂移、脚底朝向、循环接缝跳变、左右髋间距。

---

## 3.5 层级定位结果（2026-09-19 实测，脚本 `ik_60_contact_audit.py` + `ik_61_raw_dump.py`）

**结论：交叉腿是重定向层引入的，源干净；且问题不在四肢链，在骨盆/根骨级。**

`idle1` 全 65 帧、身体坐标系、按腿长归一化：

| 指标 | 源 | 产物 | 读法 |
|:---|---:|---:|:---|
| 髋间距/腿长 | 0.267 | 0.219 | 产物髋窄（与已知 22% 差一致） |
| 髋 次序差 | +0.267 | +0.218 | 髋左右正常 |
| **膝 次序差** | +0.590 | **−0.052** | 产物膝已交叉/重合 |
| **脚 次序差** | +0.723 | **−0.183** | 产物脚左右反了，65/65 帧 |
| 腿段最小距离 | 0.267 | 0.116 | 两腿贴得很近 |

原始组件空间坐标（`ik_61`，frame 0，cm）：
- 源：髋距 2657、脚距 7215，脚向量与髋向量**同向**（外展，左右各在一侧）
- 产物：髋距 20.5、脚距 34，但**两只脚都落在髋轴的同一侧** —— 左脚踏到右侧约 17cm、右脚踏到左侧 ⇒ 真交叉

**关键反常**：源与产物的**大腿方向几乎完全一致**（2D 分量 −0.879/0.477 vs −0.876/0.482，即 ik_37 的 0.01°），
但两者的**髋轴方向相差约 137°**（点积 ≈ −0.73）。
⇒ 差异不在四肢链（链方向被正确传递），而在**骨盆/根骨级的全局朝向约定**。
这与未决项「源骨架 forward 是 −Y 还是 +Y 未敲定 / 根骨那个非零偏移很可能在补全局朝向差」吻合。

**下一步（待确认再执行）**：量两侧 **rest pose** 下 `Root/root` 与骨盆的朝向差，
用一次**显式的根骨/骨盆朝向对齐**修（不是逐骨补角度），再跑 `ik_60` 看脚次序差是否转正。

---

## 4. 路线去留

- LOL 路线**值得保留但不做唯一依赖**：locomotion 优先评估 2XKO 本体
  `darius_base_nav_std_*`（已验证的第三人称动画，零重定向误差）；LOL 动作留给技能与标志性表演。
- ⚠️ **2026-09-19 核实：`darius_base_nav_std_*` 本机拿不到。** `E:\UE\Assets\Darius_God_king\`
  里只有 `God King Darius 2XKO.fbx`（模型）+ 贴图，**没有任何本体动画**；本机也没装 2XKO 客户端。
  ⇒ 这条备选要落地，前提是**先拿到 2XKO 游戏文件**再说，目前只能作为「待获取」项。
- 若必须保留 LOL 原版移动 ⇒ 承认它是「重定向 + 动作适配」，不是自动对齐一次就完。
  `run` 放慢 0.70 只降播放速度，**不会自动获得走路的支撑节奏与重心变化**。

---

## 5. 工程卫生

- 编辑器：`deno task editor:up -- --hold`（**停该后台任务 = 杀编辑器**）；`editor:status` 查网关
- 每轮 UE 操作前跑 `Scripts/editor_dialog.py --auto`（先 `WM_CLOSE`，Esc 对部分 Slate 框无效）
- 关卡基线 7 actor；跑完清临时 actor 与 `/Game/Temp*`（`plan_99_clean_temp.py`）
- git：`bb6e687`（检查点）/ `29d507a`（脚本归档）/ `70e1801`（v2+结论）；
  `main` 领先 `origin/main` 8 个提交未推送
- 交付物禁止放 `Saved/`；`Anims_TP_*` 实验目录已 gitignore（`tw_feet`、`v2` 例外）
