# 2026-09-21 工作日志

## 17:20 复盘另一个 Agent（Antigravity）的「方案 A」IK Rig / RTG 重定向整改（只读审计，未动引擎）

背景：用户 16:30 批准方案 A（DCC 空间归一化 + 定制 1:1 IK Rig），要求解决 LOL 源骨架重定向后**左右脚交叉**。
对方 16:55–17:04 跑完一条龙：`src_clean_v2.py` → `import_clean_lol_source.py`（46 动画 + 洁净网格）→
`setup_custom_ik_rigs.py` / `rebuild_ik_lol_clean.py`（源 rig 20 链）→ `refresh_rtg_rigs.py` →
`verify_and_retarget_idle.py` → `audit_retargeted_idle_frames.py` → `capture_retargeted_idle.py`。
17:15 用户 继续 后该 Agent 崩溃（Agent execution terminated due to error）。

### 真正修好的
- **RTG 脏映射已根除**：`Target[LeftFoot] ← Source[LeftFoot]` 等 8 条关键链断言全 PASS，
  不再是 `LeftFoot ← LeftRing` / `Pelvis ← LeftRing` 那种贪婪模糊匹配（日志 13619–13626）。最终显式映射 19 条。
- 源 rig 链从 13 条重建为 20 条（补 `Neck` / `LeftFoot` / `RightFoot` / `Weapon`）。

### 审计发现的 6 个硬问题（对方全部当成了通过）
1. **手性（镜像）冲突未解决**。实测组件空间 ref pose：目标 `foot_l x=+31.19`、`thigh_l x=+10.2`（+X 侧）；
   源 `L_Hip x=−9.42`、`L_Foot x=−38.76`（−X 侧）；而**两侧脚趾向量都朝 +Y**
   （目标 (2.99, 20.53, −12.72) / 源 (6.51, 8.36, −8.88)）。
   ⇒ 同名 L 骨落在相反 X 半空间 = 两套骨架**互为镜像（手性相反）**。180° 偏航只能改朝向、改不了手性。
2. **「Yaw 180 归一化」很可能被叠加了两次**：`test_yaw180_ue`（旧 FBX + 同一 import_rotation yaw=180）得
   `L_Foot=(+38.76, −41.92)`，生产导入（新 FBX + 同一 yaw=180）得 `(−38.76, +41.92)`，恰为精确取反；
   而 16:39 与 16:57 前后两次测量的源 ref pose **数值完全相同**。另注：日志显示实际走的是 **Interchange** 导入器，
   旧版 `FbxImportUI.skeletal_mesh_import_data.import_rotation` 是否生效存疑。
3. **源参考姿态仍是「扭转战斗弓步」，方案 A 自己的验收线被违反**：双脚前后差 **88.06 cm**
   （计划里写的断言是 < 1.0 cm）；且 `L_Shoulder x=+21.42` / `R_Shoulder x=+23.33` **双肩同在 +X**，
   而髋部在 ∓X ⇒ ref pose 本身躯干扭转畸形。`00_BindPose` 首 Take 的解法没用
   （UE 骨架 ref pose 来自 FBX bind 矩阵，不来自首个 Take）。
4. **retarget pose 偏移被清零后自证清零**：`reset_retarget_pose` 把偏移归零，紧接着断言
   `thigh_l yaw ≈ 0 → 左右腿交叉畸变已彻底清零`（日志 13628–13629）。这是自证循环，不是对齐验证。
   而 08:32 的 `auto_align_all_bones(TARGET)` 曾给出 `thigh_l yaw = −71.97`——该值**恰恰可能是镜像补偿**，
   被当成「畸变」清掉了。retarget pose 名字仍是 `Default Pose`，从未重建过。
5. **「0/66 交叉帧、双脚间距 0.01 cm」是无效指标**：`AnimationLibrary.get_bone_pose_for_frame` 返回
   **父级相对（local）变换**（实测 `thigh_l loc=(0.01, −0.02, 0.10)`；双脚间距 0.01 cm 物理上不可能，
   目标组件空间双脚实际相距 30 cm+）。所以 `if p_l.x < p_r.x` 比较的是两个都≈0 的局部量，结论不可证伪
   ——违反 AGENTS.md §1.2「数字必须能证伪结论本身」。正确做法：用 `AnimPose`/SkeletalMeshComponent 取
   **组件空间或世界空间**再判左右次序与间距。
6. **交付物没落盘 + 关卡没收干净**：`Content/Character/Darius/Animations/LOL_Retarget/` 目录为空，
   `A_Darius_idle1` 只存在于 `Saved/Autosaves/.../A_Darius_idle1_Auto9.uasset`（自验脚本只 load 了内存对象）。
   编辑器自 15:11 起未重启，所以还没丢。清理脚本自报 **Actor 总数 10**（基线 7，超 3 个）。

### 附带证据
- 自验截图 `a_darius_idle1_verified.png` **其实是 800×800 `gbrapf16le` OpenEXR**（文件头 `v/1 01 02`，
  `export_render_target` 因 RT 为 FloatRGBA 写成 EXR）。已用 ffmpeg 转出并落到
  `Docs/A_Darius_idle1_verified_converted.jpg`（+ `_legs_crop.jpg`）：整张是过暗剪影、头部被裁出画面、
  披风糊住双腿 ⇒ 不构成合格视觉门禁。
- 映射覆盖 19/30：未映射 = 8 个 metacarpal + Left/RightMiddle + Pelvis（人为置 None）。
  源 rig 没有 Middle 手指链、也没有 Pelvis 链（只作 retarget root），所以中指名指落回 ref pose；
  另注源 retarget root = `Pelvis`、目标 = `root`，两侧 root 处理不对称。
- `IKRetargeterController` 无 `get_retarget_op_at_index`（探测 ops 失败），RTG 里那 5 个 op 至今未知。
- `git status`：76 个已跟踪资产被删（Anims_TP / Anims_TP_v2 / Anims_TP_tw_feet 与旧 LOL_Source），
  但两个已验收交付物 `A_Darius_AxeIdle_Layered` / `A_Darius_AxeWalk_Layered` 仍在 `Anims/`，M1 地基未受损。

### 建议的下一步（等用户确认，不擅自开跑）
1. 用组件空间/世界空间重写脚部审计（左右次序 + 间距 + 与源同帧对比），替换掉那个 0.01 cm 指标。
2. 先判定手性：明确「镜像 ⇒ 交叉映射 LeftLeg←RightLeg」还是「用 auto_align 的逐骨补偿」，
   不要再把 `−71.97°` 这类补偿当畸变清零；并把 ref pose 的扭转问题当作独立课题。
3. 落盘 + 复位：`save_asset` 到 `Content/`、关卡归 7、截图改 RGBA8 或导出 .exr 再转。
4. 可证伪的质量测试：重定向 `A_LOL_Darius_run`，量目标 `thigh_l` 的局部旋转跨帧摆幅；
   若接近 0（≈停在 ref pose）则说明这次重定向只是「A-pose + 微小增量」，并非在传递动作。

---

## 17:20–17:45 接手验证：崩了一次编辑器，换了正确的验证通道，拿到铁证

### A. 事故（我造成的，记录以防重演）
- 脚本 `rtg_10_world_audit` 的第一版：先 `destroy_actor` 掉 3 个残留 `SkeletalMeshActor_0/2/3`（关卡回到 7），
  再 `spawn_actor_from_class(SkeletalMeshActor)` + `set_animation`，随后在 `set_update_animation_in_tick`
  （5.8 无此方法）抛 `AttributeError`。
- **17:29:45 编辑器 EXCEPTION_ACCESS_VIOLATION 崩溃**，栈：python311 → PythonScriptPlugin → CoreUObject → UnrealEd。
- 后果：**内存里的 `A_Darius_idle1` 丢失**（从未落盘）。
- 抢救：`Saved/Autosaves/Game/Character/Darius/Animations/LOL_Retarget/A_Darius_idle1_Auto9.uasset`（17:08）
  复制到 `Content/.../Animations/LOL_Retarget/A_Darius_idle1.uasset`，重启后用 `save_asset` 正式落盘（返回 True）。
- 教训已写进 `MEMORY.md §3b`：验证一律走 `AnimPoseExtensions` 纯求值，**不生成 Actor**。

### B. RTG 预览「看不到 2XKO 模型」的答案
- 两侧预览网格**都绑好了**：TARGET=`SK_Darius_GodKing`，SOURCE=`SK_LOL_Darius` ⇒ 不是绑定问题。
- 嫌疑最大的是 **target retarget pose 的 root offset = `(0, 0, +206.227)` cm**（目标根骨在 0，源 retarget root `Pelvis` 在 z=87）
  —— 目标被顶出取景框；同时**两侧所有骨骼的旋转偏移读回来全是 0.00**（被 `reset_retarget_pose` 清零过）。
- RTG op 栈（正确 API `get_num_retarget_ops`+`get_op_name`）= 5 个：`Pelvis Motion` / `FK Chains` / `Run IK Rig` / `Root Motion` / `Remap Curves`。
- 源 rig 有 **39 条链 = 20 条 + 19 条 `_0` 重名副本**（`LeftLeg` 与 `LeftLeg_0` 并存），需清理。
- 默认视口显示的是 **retarget pose（=两边 ref pose）**，不是动画；要验动画得在 Retargeter 里预览具体序列。

### C. 世界空间审计（9 帧 × 3 条片段；TGT 单位=米→×100）
| 观测量 | 重定向产物 `A_Darius_idle1` | 源 `A_LOL_Darius_idle1` |
|---|---|---|
| foot_l / foot_r | `(+34, −21, 24)` / `(−41, +37, 21)` cm，间距 **94 cm** | `(+28, −16, 17)` / `(−35, +19, 17)` cm，间距 72 cm |
| 左右次序 | foot_l 在 +X、foot_r 在 −X ⇒ **没交叉**（骨盆整体偏 −13.5cm） | L 在 +X、R 在 −X ✓ |
| 脚尖朝向 | `(3.5, +20.5, −12.7)` → **+Y** | `(−0.3, −10.6, −8.9)` → **−Y** |
| 全身 | pelvis z=104cm、head z=174.5cm（ref 109/188.6）⇒ 不飘、贴地 | pelvis z=117cm、head z=187cm |
| `weapon_jnt` | **全程钉死 `(−2,−4,3)` cm 不动，距 hand_r 108cm** | `Weapon` 恒距 R_Hand 17.37cm（手内刚性）✓ |
| 片段运动量 | 脚位移 ≤ 0.11 m（idle 本来就静止） | idle 0.01cm；run 91.7cm |

**结论**：① 腿**没有交叉**，但站距 94cm（自身自然站距 62cm）明显过开；② **朝向差 180°**（这是硬伤）；
③ **斧头确实躺在地上**，因为 `weapon_jnt` 父级是 `root`，FK 重定向原理上不可能把斧头带到手上；
④ idle1 是准静止片段，**不能用来判质量**，验收必须用 `run`。

### E. 17:36 截图核验：确认「反着站」是数据层面的真 180°
用户截图（关卡里两个 Actor 并排）：彩色带贴图的 = 目标 `SK_Darius_GodKing`（7 材质槽，斧头靠 `hand_rSocket` 挂在手上 ✓），
纯灰无贴图 = 源 `SK_LOL_Darius`（`import_clean_lol_source.py` 里 `import_materials=False`，所以是素模）。
图中目标只露背面（披风朝镜头），源露出胸腹正面 ⇒ 两者朝向相反。
**这与我量到的数据一致**（网格空间，不受 Actor Yaw 影响）：源 idle1 脚尖 `−Y`，重定向产物脚尖 `+Y`。
⇒ 是真 180°，不是「还没调」。修复选项：(a) RTG `TargetRootSettings.RotationOffset` 设 180° yaw（外科式，最小改动）；
(b) 两侧 retarget pose 摆同一套中立姿态后 auto_align（正统但费工）；(c) 先排除两个 Actor 的 Actor Yaw 不一致造成的画面假象。

## 17:40–18:25 「直接动手」：建验收线 + 六轮受控实验，锁定唯一有效杠杆

### 建好的东西
- `Scripts/retarget/rtg_20_fix_verify.py`：mode 从 `Saved/Attack/rtg_fix_mode.txt` 读 → 快照 RTG → 施加修复 →
  把源 `run`/`idle1` 重定向到 `/Game/Character/Darius/Animations/LOL_Retarget_Test/` → 落盘 → 量三件事打 PASS/FAIL。
- `Scripts/retarget/rtg_22_delta_probe.py`：同一 AnimPose 内比「动画姿态 vs 参考姿态」的骨骼旋转差/位移（不受坐标系干扰）。
- 备份：`Saved/Attack/backup_20260921_1740/`（RTG + 两个 IK rig 的 .uasset）；快照 `Saved/Attack/rtg_snapshot.json`。

### 基线数字（源 run vs 产物 run）
| 指标 | 源 | 产物（未修） |
|---|---|---|
| 脚尖 dot(−Y) | +0.766 | −0.841 |
| lr_score（朝向无关左右一致性） | +20.95 | **−31.16** |
| 脚位移幅度 | L 87.74cm | foot_l 109.6cm（≈1.25×，与腿长比吻合）✓ |

### 六轮受控实验（全部无效或更差）
1. `TargetRootSettings.rotation_offset = yaw180` → 数字一比特没变（被后面的 FK op 覆盖）。
2. `Pelvis Motion.rotation_offset_global = yaw180` → 位置变了（travel 109→55）但**朝向不变**。
3. 关闭 `Run IK Rig` op → travel 101.6，朝向不变。
4. FK Chains 全 31 链 `rotation_mode=ONE_TO_ONE`（`unreal.FKChainRotationMode`，注意不是 RetargetRotationMode）→ 100.8，朝向不变。
5. `auto_align_all_bones(SOURCE, CHAIN_TO_CHAIN)` → 写入了 L_Hip/R_Hip/Spine/肩 的偏移，但旋转差又回到 ~170°。
6. **源 retarget-pose `Pelvis` 旋转偏移 = yaw180** → **唯一有效的**：lr_score −31→**+31**、calf_l 旋转差 178°→**16°**、
   ball_l 170°→18°；但 thigh_r/calf_r 仍 ~170° ⇒ 单点反向不够，必须整身一致。

### 关键测量：目标姿态相对自身 ref pose 的旋转差
同帧内用 `AnimPoseExtensions.get_ref_bone_pose` 对比：目标 **pelvis 158-165°、spine 160-168°、thigh_l 174-177°、
calf_l 173-178°、ball 170-179°**；而源自身只有 **10-60°**。⇒ **~180° 是重定向注入给目标的**，不是源的属性。

### 结论与下一步
- 根因：**两侧校准姿态不对应**（源 ref pose = 88cm 扭转弓步，目标 = 干净对称 A-pose），
  所以每个骨的相对增量都带着这个巨大偏差；单旋钮（root/骨盆/IK/FK 模式）都无法消掉它。
- 正确做法：**在 RTG 里重建相互对应的校准姿态** —— 最省事的自动化路线是「以目标 ref pose 为锚，
  逐骨计算源 retarget-pose 的旋转偏移，使源在该姿态下的骨骼世界朝向与目标一致」，然后 `auto_align` 复验。
- 现场已还原原始状态（两侧 `reset_retarget_pose` + FK=Interpolated + Run IK 开 + 各偏移归零），
  复测数字与初始基线**完全一致**，没有留下半成品状态。
- 测试产物留在 `Content/.../Animations/LOL_Retarget_Test/`（A_Darius_run / A_Darius_idle1），未污染正式目录。

## 17:48–18:4x 第二轮：找到并落地「不再反面站」的修复配方（mode=`mirror`）
用户看截图后要求「继续调整让两者不再反面站」。本轮的关键突破是**共轭**：
- `set_rotation_offset_for_retarget_pose_bone` 的偏移是**骨骼自身坐标系**里的量。直接写 `Rotator(yaw=180)` 会沿着该骨的局部轴翻 →
  实测把角色掀翻（`pelvis_z=−237cm`，头朝下）。正解是把「世界空间绕 Z 转 180°」共轭进去：
  `off = qconj(R_root_refWorld) * (Rz90*…)`（具体：`off = qconj(R) * (Rz180 * R)`，R 取 `IK_Rig.get_ref_pose_transform_of_bone("Root").rotation`）。
- 单靠它还不行（`lr_score` 反而 −62），必须再叠加**全身左右链交叉映射**（26 条：腿/脚/臂/锁骨/手指 Left↔Right）。
- 两者合起来（mode=`mirror`）的验收：`hand_score` −44.46（源 −51.99，同号 ✓ 斧头回到正确的手）、
  `lr_score` +28.56（源 +20.95 ✓）、脚位移 93.1cm（源 87.7 ✓）、`pelvis_z` 76.3cm（不翻 ✓）。
- 踩坑记录：中途忘记把 **TARGET** 的姿态偏移一起归零，导致同一个 mode 跑出两套数字（污染）；已写进 hygiene。
- 另：判定朝向不能用「单只脚 foot_l 的脚尖」——交叉映射后该骨由源的**另一只脚**驱动，指标会被污染。
  可信的是 `hand_score`（斧手侧）+ 对应脚脚尖同号。这一点已写进 MEMORY.md。

## 17:55 状态建档 + 武器问题定案

### 产出
- `Docs/Retarget/RTG_LOL_To_Darius_修复记录_2026-09-21.md` —— RTG 当前可用状态的完整记录
  （两个旋钮的精确值、31 条链映射表、op 栈状态、验收数字、工具命令、回滚路径、未决项）。
- `Scripts/retarget/rtg_31_state_dump.py` —— **只读**状态快照 → `Saved/Attack/rtg_state_after_fix.json`。
  ⚠️ 注意 `rtg_20_fix_verify.py` 每次运行都会做「基线卫生」（两侧 Root/Pelvis 偏移归零），
  **不能用 mode=none 去看现状**，否则会抹掉修复。已在文档与 MEMORY.md 里标红。
- `Scripts/retarget/rtg_30_weapon_scan.py` —— 武器刚性扫描。

### 现场状态（已核实）
- `TargetRootSettings` 全 0；两侧 retarget-pose root offset 全 0。
- op：Pelvis Motion(rotation_offset=0) / FK Chains(31 链, INTERPOLATED) / Run IK Rig(开, 4 条 IK 链) /
  Root Motion(**关闭**) / Remap Curves。
- 链映射 = 交叉映射 26 条 + 同名 4 条（Spine/Neck/Head/Weapon）+ 未映射 11 条
  （8 个 `*Metacarpal`、LeftMiddle、RightMiddle、Pelvis）。

### 武器定案（46 条源片段扫描）
- **17 条刚性**（漂移 0.000cm/0.000°）：`turn*`、`idle*`、`run*`、`walk` 等移动类 → socket 挂载无损。
- **29 条有漂移**：attack1 42.3cm/82.4°、attack2 46.0cm/90.4°、crit 28.7/50.4、spell3 48.7、spell4_5 54.8/140、
  taunt 52.0/137、dance 184/101、death **404cm/179.8°**、recall 4.4/29.3 …
- ⇒ **握法在 LoL 原版里就是动画数据**。只挂 `hand_rSocket` 对那 29 条有损（斧头会被焊死在固定握法，攻击发僵）；
  移动类无损，所以 M1（idle/walk）现状可用。
- 方案（不改骨架层级）：重定向后加一道**烘焙工序**，把 `weapon_jnt` 每帧写成为 `hand_r` 世界变换 × 握持偏移
  （`AnimationDataController` 写轨）；斧头网格从 `hand_rSocket` 迁到 `weapon_jnt` 时用
  `Saved/Attack/weapon_migrate_offset.json` 的迁移偏移，门禁 `Scripts/anim/wp_20_weapon_audit.py`。

## 18:05–18:2x 用户报「导出后人物贼小 + 披风爆炸」+「武器有些是左手」→ 两个都查清
### 缩放：证明是重定向写坏了最外层骨的 scale
- 逐骨扫 scale（`rtg_33_scale_hunt.py`）：重定向产物 **310 轨**，最外层骨 `darius_godking_mesh_LOD0_Skeleton`
  的 scale = **1**；好样本（`A_Darius_AxeIdle_Layered` / `AxeWalk_Layered`）该骨 = **100**（且只有 309 轨）。
  ⇒ 角色被渲染成 **1.85cm**；Chaos Cloth 约束距离按 100× 单位写死 ⇒ **披风撑爆**。AGENTS.md §4 早记过这条坑，今天复现。
- 工程里**已有**对症脚本 `Scripts/retarget/ik_31_fix_root_scale.py`（含三条踩坑记录）。我按同法写了
  `Scripts/retarget/rtg_41_fix_outer_scale.py`（目录可配、幂等、副本先验），在测试产物上跑通：
  **scale 1.0 → 100.0，不同姿态 5 → 5（动画没被清空）**，已落盘复核通过。
- 结论：**每次批量重定向之后必须紧跟这道后处理**，否则导出的动画全是 1.85cm + 爆炸披风。
### 武器手别：源是右手，问题出在我加的「手臂交叉映射」
- 扫 46 条源片段 `Weapon` 到两手距离：**44 条更近右手**（dR 16.8–32.9cm vs dL 38–174cm），
  只有 `death`（264.8/380.0，斧头已脱手）和 `joke_loop`（158/170，双手）模糊 ⇒ **源就是右手武器**，与 `hand_rSocket` 一致。
- "看起来是左手"的真因：mirror 把**手臂也交叉**了 ⇒ 目标持斧的 `hand_r` 由源的 **L_Hand** 驱动：
  侧别是对的（`hand_score` −44.5 vs 源 −52.0），但**运动来源是另一只手**，非对称攻击里就像拿错手。
- 干净复跑对照（hygiene 修好后）：
  | mode | 腿 | 臂 | lr_score | hand_score |
  |---|---|---|---|---|
  | yaw(src) + 全同名 | 同名 | 同名 | −62.5 ✗ | — |
  | yaw(src) + 腿交叉 | 交叉 | 同名 | −51.9 ✗ | +48.8 ✗ |
  | **mirror**（yaw(tgt) + 全交叉） | 交叉 | 交叉 | **+28.6 ✓** | **−44.5 ✓** |
  ⇒ 定稿仍用 **mirror**（用户已确认可用）。手臂是否拆开需再测「目标 hand_r 与源 R_Hand/L_Hand 的运动相关性」。
- 本轮末尾已把现场恢复成 `mirror` 并重新修好缩放（两个测试片段复核 scale=100、仍在动）。

## 18:15–18:3x 手指优化：补中指链 + 用 Blender 查清源资产天花板
### 审计（`rtg_50_finger_audit.py`）
- 源骨架 66 骨 / 手指 20 根（每指 2 节）；目标 309 轨 / 手指 38 根（`*_metacarpal_l/r` + `*_01/02/03_l/r`，拇指 3 节）。
- 链映射缺口：两侧 `Middle` + 8 个 `*Metacarpal` 全是 `None`；`Pelvis` 也 `None`。
- 活动量（相对参考姿态的最大旋转差，含整身贡献，不可横向跨骨比较，但可同骨前后对比）：
  中指 `middle_01/02/03_l` = 111.7 / 108.4 / 103.7°（≈ 冻结基线，掌骨同为 102–108°），
  而食指 `index_01/02/03_l` = 144.7 / 166.9 / 167.4° ⇒ **中指确实没被驱动**。
### 修复
- `rtg_51_add_middle_chains.py`：源 rig 补 `LeftMiddle`(L_Middle1→L_Middle2) / `RightMiddle`(R_Middle1→R_Middle2)，39 → 41 条链，已存盘。
- 重跑 `mirror`（自动按镜像规则映射两条新链）+ 重跑缩放修复。
### 复测
- 左手中指 111.7/108.4/103.7 → **142.5 / 158.4 / 160.8°**；右手中指 169.4/169.4 → **178.4 / 166.9°**（与食指/无名指同级 ✓）。
- 掌骨仍 102–108°（未映射、保持冻结），符合预期。
### Blender 直查原始 GLB（`Scripts/dcc/blender_50_finger_probe.py`）
- `darius_skin15_all_anims.glb`：**179 骨**，手指 **20 根**，`per_finger_counts` 全部 = 2，**没有掌骨**；
  手指直接挂在 `L_Hand`/`R_Hand` 下（R_Hand 下还有 `Weapon`、`SnapWeapon2Hand`、`Axe_Handle`、`*_Buffbone_Glb_Hand_Loc`）。
- ⇒ **LoL 原版就只有 2 节手指骨**，我们的白名单没删过东西；"源 2 节 → 目标 3 节"的插值分布是源资产天花板。
- 8 个掌骨在源里没有对应骨 ⇒ 无法 1:1 重定向，只能保持冻结或做高风险近似。
- 结论：手指优化到此为止（节奏已对齐）；继续拧只能试手指链的 `rotation_mode`（INTERPOLATED → MATCH_SCALED_CHAIN 等），
  收益有限，优先级低于武器/战斗。







### D. 新根因（比之前那套诊断更致命）
**LoL 源动画整体带 180° yaw**：源 ref pose 脚尖 `+Y`、`L_Foot x=−38.8`；源 idle1 动画脚尖 `−Y`、`L_Foot x=+28.2`。
⇒ 拿 ref pose 判朝向/左右/镜像的整套结论都不成立；而 RTG 的 retarget pose 又被 reset 成 ref pose，
**校准基准与所有动画差 180°**。要修的是校准姿态（两侧摆同一套中立姿态后 auto-align），不是 DCC 里转模型。

---

## 18:28–18:5x scale=100 治理：从「反复踩坑」到「唯一常量源 + 门禁」

用户问「scale=100 的各种问题是不是遇到太多了，有安全方法彻底修复吗」。先做**只读调查把账算清**，再决定动不动手。

### 调查结论（全部实测，零写入）
- **100 挂在哪**：唯一载体 = 骨架最外层骨 `darius_godking_mesh_LOD0_Skeleton`，`local scale = 100.0000`、`loc=(0,0,0)`。
  其余全骨（含 `root`/`pelvis`/`hand_r`/`weapon_jnt`）**local scale 全是 1.0，local 平移是「米」级**
  （`pelvis` loc=(−0.0619,−0.9415,0.0166)、`hand_r` y≈−0.3401、`weapon_jnt` y≈−0.0320）。
- **网格顶点是 cm 级**：`mesh.get_bounds()` origin z=105.02、extent z=101.20 ⇒ **全高 202.4 cm**。
  ⇒ 世界 = 局部 × 100，视觉正确。**100 不在几何上，在骨骼表示上。**
- **影响面（爆炸半径）**：**60 条 AnimSequence**（13 条目标骨架 + 47 条源骨架）、1 BlendSpace、
  1 Blueprint、`Cloth/CA_Darius_Cape`（ChaosClothAsset 确实存在）、`SK_Darius_GodKing_Physics`、7 材质槽、2 Skeleton。
- **没有 .blend 持久工程**：所有 DCC 处理都是 `blender -b -P 脚本` 从 GLB 现算 ⇒ DCC 侧可脚本化重跑。

### 「根治」的可行性（本轮关键新发现）
- 数学上「最外层骨 scale→1 + 其余全骨 local 平移 ×100」是**世界等价**的：
  UE 的 bind matrix 由 Skeleton rest **实时算出**（不烘在网格里），改造后 `bone_world_bind` 不变 ⇒ **顶点与 bind 零变化**。
- 引擎内**确实有 API 可实施**：`unreal.SkeletonModifier`（43 成员）提供 `set_bone_transform` / `set_bones_transforms` /
  `commit_skeleton_to_skeletal_mesh`；`Skeleton.get_reference_pose()` 可读参考姿态。
- **但代价是「重做地基」**：60 条动画 local 平移重写（压缩动画要逐帧取）+ `CA_Darius_Cape` 布料约束/权重按 100× 写死
  ⇒ 等于重调布料（最不可控）+ PhysicsAsset body 重标定 + socket/组件参数 + **M1 全量重验收**。
- **判断**：在 M1 已验收、正要转 C++ 战斗的节点上，风险收益比不成立 —— 收益只是「少记几个数」。真要动，
  先复制骨架+网格+2 条动画做**副本验证**。这套判断已写进 `AGENTS.md §2.4`。

### 落地的东西（本次交付）
- `Scripts/core/fight_scale.py` —— **唯一常量源**：`OUTER_BONE`/`OUTER_SCALE`/`LOCAL_PER_CM`/`cm_to_local()`/
  `local_to_cm()`/`SOCKET_SCALE`/`outer_scale_ok()`/`socket_scale_ok()`，并写明四类溢出。**禁止再写裸 100/0.01/1.0967。**
- `Scripts/core/scale_audit.py` —— **只读门禁**，输出 `VERDICT: PASS/FAIL`：
  ① 扫全部目标骨架动画的最外层骨轨道（无轨道 = 回落 rest 100，合规；有轨道必须 = 100）；
  ② `hand_rSocket` 的 relative_scale 必须 = 0.01；③ 多骨 × 真实帧长的活性检查（< 0.5° 判疑似静止）。
- 只读探针：`retarget/rtg_60_scale_anatomy.py`（层级与挂点）、`rtg_61_scale_impact.py`（影响面）、
  `rtg_62_rest_api_probe.py`（SkeletonModifier 等 API）、`rtg_63_evidence.py`（rest 真值/socket/布料/物理）、
  `rtg_64_cloth_state.py`（布料资产定位）。
- `AGENTS.md`：新增 **§2.4 scale=100 契约**（载体 / 四类溢出 / 常量源 / 门禁 / 为何不清零）；
  §2.1 与 §4 红线表加了指针；§3 工具链表加 `core/` 两个新脚本。

### 门禁首次运行结果（13/13 合规）
- 13 条目标骨架动画：最外层骨**全部合规**（8 条无轨道回落 100.0；5 条有显式轨道且 = 100.0，属 Layered 系）。
- `hand_rSocket` relative_scale = **(0.0100, 0.0100, 0.0100)** ✓。
- 47 条源骨架动画被正确跳过（不属于目标骨架契约）。
- **副产品发现**：`A_Darius_AxeIdle_Layered` 的跨帧最大旋转差只有 **0.64°**（`AxeJump_Loop` 0.92°、
  `AxeIdle_Mixamo` 2.62°）⇒ 待机几乎是静止姿势；若想要「呼吸感」得另做，这是数据支持。
- 门禁自身修过两处：`unreal.Quat` **没有 `dot()`**（改手算点积）；活性判据第一版用固定帧 + 单骨 + 6 位小数，
  对 idle 误报（改成多骨 + 真实帧长 + 角度阈值 < 0.5° 才算静止）。

### 未覆盖（诚实标注，不伪装成已检查）
- 蓝图组件的相对变换：`Blueprint.simple_construction_scripts` 不从 Python 暴露、CDO 上也取不到组件
  ⇒ 门禁里明确标注「未覆盖」。战斧的真正载体是 socket（已覆盖）。

---

## 23:20 斧头挂点裁决：RTG 把源 Weapon 送错了骨（`weapon_jnt` 应为 `weapon_jnt_r`）

### 根因（纯静态取证即定死，无需跑实验）
- 目标骨架 `SK_Darius_GodKing` 自带**三个**武器锚点：`weapon_jnt`(父 **`root`**) / `weapon_jnt_r`(父 **`hand_r`**) / `weapon_jnt_l`(父 `hand_l`)。
- 源 `SK_LOL_Darius` 武器骨层级 = `Axe_Head ← Weapon ← R_Hand` ⇒ 与源**同构**的是 **`weapon_jnt_r`**。
- 但 `Scripts/retarget/clean_weapon_chains.py` 把 IK_Darius 的 Weapon 链建在 **`weapon_jnt`** 上，
  RTG 里 `"Weapon": "Weapon"`（见 `Saved/Attack/rtg_snapshot.json`）。**两侧层级不对称**
  （源挂 `R_Hand`、目标挂 `root`）⇒ FK 重定向写出的局部变换被当成「相对 root」⇒ 产物轨全程恒定、
  锚点钉死在脚下 `(−2,−4,3)cm`（离 `hand_r` **108cm**）—— 这就是「插地死斧」。
- 佐证：`weapon_track_audit.json` 里**全部**重定向产物的 `weapon_jnt` 局部平移行程 = **0.000**。

### 为什么现在「表现力尚可」
- 斧头几何挂在 **`hand_rSocket`**（`hand_r` 上的 socket）= **刚性**挂点 ⇒ 100% 跟手但**握法是常量**。
- 源里握法**是动画数据**：46 条片段 17 条刚性（idle/walk/run/turn）、**29 条有漂移**
  （attack1 相对手 82°、spell4_5 140°、death 脱手 404cm）⇒ socket 只覆盖 17/46，攻击类会僵。

### 交付
- `Scripts/anim/wp_40_weapon_anchor_recon.py` —— **只读裁决**：A 两侧武器骨父级 / B IK 链定义+RTG 映射+FK op 武器链设置 /
  C 产物 `weapon_jnt|weapon_jnt_r` 局部平移行程 / D 世界空间「锚点↔手」距离（目标米×100 vs 源 cm）。
  输出 `Saved/Attack/weapon_anchor_recon.json`。
- `Scripts/anim/wp_41_repoint_weapon_chain.py` —— 把 Weapon 链 start/end 骨改成 `weapon_jnt_r`。
  **默认 dry-run**（要写需把 `Saved/Attack/wp41_mode.txt` 写成 `apply`）；自动备份 IK_Darius、幂等、写完自检。
  链名不变 ⇒ RTG 的 `Weapon ← Weapon` 映射不用动；**不动骨架层级** ⇒ 100×/socket/布料/物理契约零影响。
- `Docs/Attack/斧头挂点修复_weapon_jnt_r_方案.md` —— 根因/证据/5 步修复/备选烘焙/门禁。

### 两个已知的坑（已写进方案）
- `weapon_jnt_r` 是**单骨链**（start=end）⇒ `Direction`/`CHAIN_TO_CHAIN` **从原理上定不出方向**（同 hand/foot 坑），
  retarget pose 必须 `Align Selected` 或视口手转。
- Parent Socket **无 Python API**，第 5 步只能手点；偏移用 `weapon_migrate_offset.json` 的 `PROPOSED_weapon_jnt_rel`
  （那套值本就按 `weapon_jnt_r` 的局部量推的，正好对得上）。

### 备选（Step 2 手对不齐时）：离线烘焙
`L_grip(t) = W_R_Hand_src(t)⁻¹∘W_Weapon_src(t)`；`M_hand = W_hand_r_tgt(t₀)⁻¹∘W_R_Hand_src(t₀)`；
`L_jnt(t) = M_hand ∘ L_grip(t)`。副产品：这样烘焙时斧头组件相对变换退化为**单位阵 + scale 0.01**。

### 状态
**编辑器当时未开（UDP 6766 无实例）⇒ 脚本就位但尚未执行**，等确认后再启动。

---

## 23:27 概念澄清：武器挂点 ≠ 脱离 UE 武器系统

- **三层结构**：L1 骨骼/表现层（`weapon_jnt_r` 等锚点 + 握法动画数据）→ L2 挂载层（`AttachToComponent`，**唯一改动点**）
  → L3 GAS 逻辑层（ASC / GA / Tag / Effect，**完全不感知挂点名字**）。
- `weapon_jnt_r` 是**正常骨骼**：`DoesSocketExist` / `GetSocketLocation` / `AttachToComponent(socket=...)` 全部照用
  （UE 的 socket 名语义允许直接用骨名）⇒ **引擎能力零损失**。
- **关键区分不是「挂哪个骨」，而是「武器是不是独立 Actor」**：现状是 Character 上的 `UStaticMeshComponent`；
  战斗阶段建议重构 `AFightWeapon`（Lyra / GASShooter 范式，自带 mesh + collision + `UWeaponDataAsset`）——
  但它一样是 `AttachToComponent(CharMesh, "weapon_jnt_r")`，与本次改锚点**正交**，不返工。
- **握法变数据对 GAS 是加分**：`AnimNotifyState` 逐帧取 socket transform 做 sweep 时，
  若斧头视觉转 82° 而 socket 钉在 `hand_r`，hitbox 轨迹与视觉不同步（难查的「打空气」）。
- **三锚点本就是为战斗设计的**：`weapon_jnt_r/l` = 握持态、`weapon_jnt`(父 root) = **脱手/插地态**
  ⇒ GAS 做缴械 / 投掷 / 死亡脱手时的状态切换落点（源 `death` 斧头脱手 404cm 正是这个场景）。
- **唯一真实返工点**：要让武器参与 Chaos 物理（拖地/甩动）需独立 Actor + 物理约束 —— 与锚点选择无关。

---

## 23:40 🔴 重大更正：锚点是 `weapon_jnt_l`，不是 `weapon_jnt_r`；斧头一直在**错误的手**上

### 判据（`Scripts/anim/wp_45_lateral_verdict.py`，idle 静止帧 + 髋轴投影）
| 骨 | 投影 | 侧 |
|---|---|---|
| 源 `R_Hand` | **+48.4** | 右（自证） |
| 源 `L_Hand` | −52.4 | 左 |
| 源 `Weapon` | **+41.0** | **右** = 斧头在右手 |
| 目标 `hand_r` | **−51.3** | 左 |
| 目标 `hand_l` | **+45.3** | **右** = 持斧手 |

两条独立目标动画一致；源/目标数值一一吻合（48.4↔45.3、−52.4↔−51.3）。
⇒ **`SK_Darius_GodKing` 的 l/r 命名与解剖左右相反**（与 mirror 映射 `Target[LeftArm] ← Source[RightArm]` 自洽）。
⇒ **斧头挂 `hand_rSocket`（父 `hand_r`）= 解剖左手，与源相反。这是从第一天就存在的 bug**，
   之前没暴露是因为攻击动画刚做、还没视觉验收。

### 已执行
- `IK_Darius` 的 `Weapon` 链：`weapon_jnt` → **`weapon_jnt_l`**（`wp_41`，已备份 `Saved/Attack/backup_20260921_234112/`）
- 重跑 `rtg_20_fix_verify.py`（mode=mirror）：26 条交叉映射全 ok，2 条产物重导到 `Animations/LOL_Retarget_Test/`
- `rtg_41_fix_outer_scale.py`：假 scale 轨已修（1.0 → 100.0）

### 🔴 新卡点：RTG 的 FK op 根本不含 Weapon 链
- 改链后重跑的 2 条产物（`A_Darius_idle1` / `A_Darius_run`）里，
  **`weapon_jnt` / `weapon_jnt_r` / `weapon_jnt_l` 三个锚点平移与旋转全为 0**（完全没被驱动）。
- 而改链前的旧产物（`Anims/A_Darius_Attack1_LOL` 等）里 `weapon_jnt` 有行程（1.986 / 68°）
  ⇒ 说明 FK op 的 `chains_to_retarget` 列表**不含 Weapon 链**，改链后连原来那条也不写了。
- `Scripts/retarget/rtg_20_fix_verify.py` 只设链映射，不动 FK 的链列表。

### 下一步（二选一）
- **(a) 同步 RTG 的 FK op 链列表**：找 API 让 `chains_to_retarget` 重新从 IK Rig 派生（或 remove+add FK op）。
- **(b) 离线烘焙（推荐）**：绕开 RTG，逐帧
  `L_grip(t) = W_R_Hand_src(t)⁻¹ ∘ W_Weapon_src(t)`；
  `K = W_hand_l_tgt(t₀)⁻¹ ∘ W_R_Hand_src(t₀)`；
  `L_jnt(t) = K ∘ L_grip(t)` 直接写 `weapon_jnt_l` 的局部轨。完全对标源、可回归。

### ⚠️ 单位校准（本次实测，与旧记忆冲突，以实测为准）
`AnimPoseSpaces.WORLD` 的**平移数值对本骨架就是 cm**：
`thigh_l−thigh_r` 轴长读数 20.48（= 20.5cm 髋宽）、`hand_l` 离 pelvis 投影 +45.3（= 45cm）。
**不要按旧记忆乘 100**（`wp_40` 的 D 段原有 `unit_scale=100` 已修正为 1.0）。

---

## 23:55 ✅ 烘焙完成：绕开 RTG，直接用源数据驱动 `weapon_jnt_l`

### 路线：离线烘焙（`Scripts/anim/wp_50_bake_weapon_grip.py`）
RTG 的 FK op 不含 Weapon 链，与其找同步 API，不如直接用源数据：

```
L_grip(t) = W_R_Hand_src(t)⁻¹ ∘ W_Weapon_src(t)     # 斧头在源右手系里的位姿
K         = (R_hand_l_tgt(t₀)⁻¹ ∘ R_R_Hand_src(t₀), 平移置零, scale 0.01)
L_raw(t)  = K ∘ L_grip(t)                            # 源武器骨在目标右手系里的完整位姿
→ 写进 weapon_jnt_l 的局部轨
```

**两个必须记住的坑**：
1. 🔴 **`K` 的平移必须置零**。第一版用完整变换 `W_hand_l_tgt(t₀)⁻¹ ∘ W_R_Hand_src(t₀)`，
   把两个骨架 root 的绝对位置差（0.2~1.3 m）灌进锚点，`|L_raw(t0)|` 飙到 1.28 m 且各动画不一致。
   只用旋转后，`|L_raw(t0)|` = 0.174/0.195/0.126/0.174 m，**与源 `Weapon↔R_Hand` 的 17.37/19.51/12.63/17.37 cm 逐一精确吻合**。
2. 🔴 **重跑前必须 `remove_bone_track`**，否则读到的 f0 是上次烘焙值 ⇒ 复合污染
   （实测 `rest_l` 从 0.004444 变成 0.1104，与 `wp_12` 踩过的是同一类坑）。

`weapon_jnt_l` 的 rest 局部平移 = 0.004444 m；烘焙后 t0 值 = 0.174 m（源武器骨离手距离）。

### 验证
- 4 条动画全部 `set_bone_track_keys -> True` + `save -> True`：`A_Darius_idle1` / `A_Darius_run`
  / `A_Darius_Attack1_LOL` / `A_Darius_Attack1_ToIdle_LOL`
- attack1 的**旋转增量 82.42°** ↔ 源 `Weapon` 相对 `R_Hand` 的 82.4° ✓
- 源 `Axe_Head` 相对 `Weapon` 在 attack1 全程只变 **0.0097 cm** ⇒ 是**纯固定挂点**（不是动画骨）

### BP 侧（`Scripts/anim/wp_51_axe_rel_migrate.py`，已写，备份 `backup_20260921_234909`）
`WeaponAxe` 的相对变换改成「axe mesh 相对武器骨」的固定偏移：
`loc = (0, -0.838344, 0.124911)`（= 源 `Axe_Head` 相对 `Weapon` 的 84.76 cm × 0.01）、
`rot = 单位阵`（源里两者朝向一致）、`scale = (0.01, 0.01, 0.01)`。

### 🔴 还差一步手工（`wp_52` 实测确认无 Python API）
`BP_DariusCharacter → Components → CharacterMesh0 → WeaponAxe → Details → Parent Socket`：
`hand_rSocket` → **`weapon_jnt_l`**。
⚠️ **改 socket 之前斧头会用新值挂在旧 socket 上 ⇒ 位置会飞**，必须先改这一项。

### 遗留风险
`F.loc` 假设「我们的 `SM_Darius_GodKing_Axe` 原点 = 源的 `Axe_Head` 骨」。若 mesh 原点在斧头中部，
位置会偏半个斧长 ⇒ 需 PIE 目视一次再微调（只改 BP 的 `relative_location`，不动烘焙）。

### 附带结论
`RTG` 预览里**看不到斧头是正常的**：RTG 只加载 `USkeletalMesh`，不加载蓝图上的 `WeaponAxe` 组件；
源侧看得见是因为 LoL 的斧头几何画在蒙皮网格里（绑 `Axe_Head`）。

---

## 00:05（09-22）46 条全量导出 + 假 scale 轨修复 + 全量握法烘焙

### 症状归因：「人物特别小 + 披风撑爆拧成一团」= 同一个根因
RTG 的 Export / 批量重定向会给最外层骨 `darius_godking_mesh_LOD0_Skeleton` 写一条 **scale = 1** 的假轨
（正常回落 rest = **100**）⇒ ① 角色渲染成 1.85cm；② Chaos Cloth 约束距离按 100× 写死 ⇒ 披风被撑爆。
**导出与删假轨必须成对执行。**

### 执行
1. `Scripts/anim/wp_60_batch_retarget_all.py`：把 `LOL_Source/` 全部 **46 条**批量重定向到
   `/Game/Character/Darius/Animations/LOL_Retarget/`（命名 `A_LOL_Darius_x` → `A_Darius_x`），
   用 `unreal.IKRetargetBatchOperation.run_batch_retarget(IKRetargetBatchOperationInputs)`。
2. `Scripts/retarget/rtg_41_fix_outer_scale.py`（`rtg_scale_fix_dir.txt` 已改指向 `LOL_Retarget`）：
   **46/46 `scale 1.0 → 100.0`**。43 条"不同姿态 > 1"通过；
   `A_Darius_turn0/turn_l/turn_r` 报"不同姿态 1"——**不是失败**，是源里本身就没有骨骼数据的空片段。
3. `Scripts/anim/wp_50_bake_weapon_grip.py` 的 `PAIRS` 改成**自动配对**
   （扫 `LOL_Retarget/A_Darius_x` ↔ `LOL_Source/A_LOL_Darius_x`）⇒ **43 条写入握法烘焙**，
   3 条空片段自动跳过并清轨。

### 🔴 两个新踩的坑（已写进脚本）
1. **骨名列表必须取 `data_model_interface`**：`anim.get_editor_property("model_interface").get_bone_track_names()`
   **不含最外层骨** ⇒ 整名匹配删 0 条。正解 =
   `anim.get_editor_property("data_model_interface").get_bone_track_names()` + 子串 `"lod0_skeleton"` 匹配。
2. **`grep ... | head -N` 会 SIGPIPE 杀掉远程执行脚本**，导致后半段（落盘报告）没跑。
   要看日志请重定向到文件再 `tail`。

### 仍待手工（唯一剩项）
`BP_DariusCharacter → CharacterMesh0 → WeaponAxe → Details → Parent Socket`：
`hand_rSocket` → **`weapon_jnt_l`**（`wp_60` 末尾复查确认仍是旧值；`AttachSocketName` 无 Python API）。
⚠️ 改 socket 前斧头会用新相对变换挂在旧 socket 上 ⇒ 位置会飞。

